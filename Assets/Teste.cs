using UnityEngine;

public class Teste
{
   public class TextRodandonizada : MonoBehaviour
   private void Start()
   {
	   VlText.text +novoTeste * = = *numero;

	   void update()
	   {
		   if(input.GetKeyDown(keyCodelpace) && numero > 0)
		   {
			   numero ==;
			   VlText.text + novoTeste * = = *numero;
		   }
	   }
   }
}
